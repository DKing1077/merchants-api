Repository audit findings for this repository

1. Missing configuration module
   - Files: app/main.py, app/db/database.py
   - Both files import app.core.config.settings, but app/core/config.py does not exist.
   - Result: the app and test suite fail during import with ModuleNotFoundError.

2. Refund records do not get an ID
   - File: app/db/models/models.py
   - The Refunds.id column has no default value.
   - File: app/services/refunds_service.py
   - create_refund() creates a Refunds row without assigning refund.id.
   - Result: refund creation can fail on insert because the primary key is missing.

3. Refund routes are double-prefixed
   - File: app/main.py
   - The refunds router is included with prefix /v1/refunds.
   - File: app/api/routes/refunds.py
   - The router itself already uses prefix /refunds.
   - Result: endpoints resolve under /v1/refunds/refunds instead of the cleaner /v1/refunds paths.

4. Timestamp routes exist but are never registered
   - File: app/api/routes/timestamps.py
   - File: app/main.py
   - The timestamp router exists, but app/main.py never includes it.
   - Result: created_at and updated_at endpoints are dead code.

5. Ledger account schema does not match the database model
   - File: app/schemas/ledger_schemas.py
   - LedgerAccountCreate and LedgerAccountResponse require a name field.
   - File: app/db/models/models.py
   - LedgerAccount has merchant_id, account_type, currency, and created_at, but no name column.
   - Result: /v1/ledger/accounts can fail when the route passes the schema fields into the SQLAlchemy model.

6. Payment capture depends on ledger accounts that are never auto-created
   - File: app/services/payment_service.py
   - capture_payment_intent() requires cash and merchant_payable ledger accounts before a capture can succeed.
   - Result: a payment can be confirmed but capture returns an InvalidPaymentIntentStateError unless those accounts were created separately first.

7. Refund service and refund tests disagree on function signatures
   - File: app/services/refunds_service.py
   - create_refund() is defined as create_refund(db, payment_intent_id, refund_amount, idempotency_key=None).
   - File: app/tests/test_refunds.py
   - Several tests call it as create_refund(payment_intent_id, refund_amount, db).
   - Result: once imports are fixed, those tests will still fail because the arguments are in the wrong order.

8. There is a stale payment test module
   - File: app/tests/test_payments.py
   - The file references payment_service.payment_intents.clear(), but the current service is database-backed and has no payment_intents list.
   - It also expects older route behavior such as different status codes and payload shapes.
   - Result: this test file looks out of date relative to the current implementation.

9. Test tooling is incomplete in the repo
   - File: requirements.txt
   - pytest is not listed even though the repository has pytest-based tests in app/tests.
   - Result: a fresh environment does not have the repo's test runner installed by default.

Validation note:
- Command run: python -m pytest -q
- Result: collection failed with ModuleNotFoundError: No module named 'app.core.config'
